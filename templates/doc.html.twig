{% extends "base.html.twig" %}

{% block title %}Ingrid's game{% endblock %}

{% block body %}
    <h1>Ingrid's game</h1>
    <h2> The classes <h2>
    <h3>Card</h3>
    <p> Handles the individual cards and their values</p>
    <h3>DeckOfCards</h3>
    <p> Composition of cards to create a standard deck where you can draw and shuffle the cards </p>
    <h3>CardHand</h3>
    <p> Composition of cards to create a set of the deck </p>
    <h3>Player</h3>
    <p> A player which has a score in the game, can be used for both players and banks</p>
    <h3>Game</h3>
    <p> A composition of players which stores the bank and the player which has scores 
        and can determine the winner</p>

    <a href="{{ path('game_play') }}">Start game</a>
    <h2> Pseudo code over the classes</h2>
    <p> Class Game extends Player <br>
        $players[] <br><br>
        function GetBank() <br>
        return bank <br> <br>
        function GetPlayer() <br>
         return Player <br> <br>
        function winner() <br>
        <pre>   if player_score > 21: <br>
                winner = bank <br>
            if player_score <= bank_score <br>
                winner = bank <br>
            else <br>
                winner = player <br>
        </pre>
    </p>

    <p> 
        Class Player <br>
        $score <br><br>
        function add_to_score(num) <br>
            return $score += num <br><br>
        function get_score <br>
             return $score <br>
    </p>
<h2> Flow chart over the routes </h2>
    <img src="{{ asset('img/IMG_0254.jpg') }}"  alt='flowchart' width=750>
{% endblock %}
